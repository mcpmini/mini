{
  "description": "Fork a GitHub repository",
  "inputSchema": {
    "type": "object",
    "properties": {
      "owner": {"type": "string", "description": "The account owner of the repository to fork."},
      "repo": {"type": "string", "description": "The name of the repository to fork."},
      "organization": {"type": "string", "description": "Optional parameter to fork the repository into an organization."},
      "name": {"type": "string", "description": "When forking from an existing repository, a new name for the fork."},
      "default_branch_only": {"type": "boolean", "description": "When forking an existing repository, fork with only the default branch.", "default": false}
    },
    "required": ["owner", "repo"]
  }
}
